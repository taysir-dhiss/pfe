{
  "name": "cancer-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":         "concurrently -n BACK,FRONT -c magenta,cyan \"npm run server\" \"npm run client\"",
    "server":      "cd backend && npm run dev",
    "client":      "cd client && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../client && npm install"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
